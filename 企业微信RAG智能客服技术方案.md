# 基于企业微信构建 RAG 知识库智能客服系统 —— 技术方案

> 版本：v1.0 ｜ 日期：2026-08-12 ｜ 依据：企业微信开发者中心官方文档（developer.work.weixin.qq.com）
> 关键文档 path：`91039`(获取access_token)、`90930`(回调配置)、`101463`(智能机器人长连接)、`100030`(管理企业知识集)、`90236`(发送应用消息)、帮助中心`21663`(智能机器人使用)

---

## 0. 结论先行：两条技术路线

| 对比项 | 路线 A：企微原生「智能机器人+知识集」（低代码） | 路线 B：自建应用 + 自研 RAG 链路（完全可控） |
|---|---|---|
| RAG 引擎 | 企业微信官方内置，上传知识即用 | 自己搭建：向量库 + Embedding + LLM |
| 回复模型 | 企微内置混元 / DeepSeek（含深度思考模型） | 任意 LLM（通义/DeepSeek/开源本地部署等） |
| 知识库 | 企微「知识集」：在线文档/微盘/本地文件/网页/API | 自建向量数据库，完全私有 |
| 开发量 | 极低，半天可跑通 | 高，需自研全链路 |
| 多轮对话 | 内置（最多 10 轮） | 自建会话管理 |
| 转人工 | 内置关键词/无法回答自动转人工 | 自研 |
| 适合场景 | 快速验证、文档知识充足 | 私有化、深度定制、复用已有 RAG 资产 |

**推荐**：先用路线 A 快速验证价值；对回答质量、私有化、模型选型有更高要求时，升级到路线 B（长连接 API 模式不变，只替换中间 RAG 引擎）。下文以**路线 B 为主线**完整展开，路线 A 见附录一。

---

## 1. 整体架构与全链路流程

```
企业微信客户端（员工单聊@机器人 / 内部群聊@机器人 / 应用会话）
        │ 消息/事件
        ▼
接入层（企业微信开放能力）
  ● 智能机器人长连接 wss://openws.work.weixin.qq.com（首选，双向通道）
  ● 或 应用消息回调（HTTPS URL + Token + EncodingAESKey）
  ● 获取 access_token（corpid + secret）
        │ 解密/验签后的用户消息（msgid 幂等去重）
        ▼
核心服务层（自建 RAG 服务）
  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
  │ 消息路由   │──▶│ 会话管理  │──▶│ RAG 引擎  │──▶│ 回复生成  │
  │(去重/路由)│   │(多轮上下文)│   │(检索增强) │   │(LLM调用) │
  └──────────┘   └──────────┘   └──────────┘   └──────────┘
                                        ▲
  ┌──────────┐   ┌──────────┐   ┌───────┴────────┐   ┌──────────┐
  │ 知识库接入 │   │ 文档处理  │   │ 向量检索(FAISS/ │   │Embedding │
  │(离线管道) │──▶│(切分/清洗)│──▶│ Milvus/企微知识集│──▶│  模型     │
  └──────────┘   └──────────┘   └────────────────┘   └──────────┘
        │
        ▼ 回复结果（流式 / 模板卡片 / 带引用来源）
企业微信 API 回写：aibot_respond_msg（长连接）/ message/send（应用消息）
```

### 全链路时序
```
用户提问 → ①消息进企微 → ②推送给自建服务（长连接/HTTPS回调）
 → ③验签解密 → ④msgid去重 → ⑤会话管理(取/存上下文)
 → ⑥RAG检索(问题向量化→Top-K召回→可选重排)
 → ⑦组装Prompt(系统提示词+检索片段+历史+问题)
 → ⑧调用LLM生成(可流式) → ⑨回复回写企微 → ⑩用户看到回答
```

### 离线知识库管道
```
企业知识源(FAQ/产品文档/工单/规范)
 → 文档采集(PDF/Word/Markdown/Excel/网页/企微文档)
 → 文本清洗(页眉页脚/表格转Markdown/乱码)
 → 文档切分(Chunk) → Embedding向量化 → 向量入库+元数据
 → 定期增量更新(定时任务/文件监听/Webhook)
```

---

## 2. 需要调用的企业微信 API 清单

### 2.1 基础鉴权
| API | path | 作用 |
|---|---|---|
| 获取 access_token | 91039 | 一切接口前置凭证。GET `qyapi.weixin.qq.com/cgi-bin/gettoken?corpid=ID&corpsecret=SECRET`；有效期 7200s；按应用独立缓存、失效自动重取；**必须存后端，禁止下发前端** |

### 2.2 消息接收（用户问什么）
| API | path | 说明 |
|---|---|---|
| 智能机器人-长连接（首选） | 101463 | 机器人配置页开启「API 模式」+「长连接」：连 `wss://openws.work.weixin.qq.com` → 发 `aibot_subscribe`(BotID+Secret) → 收 `aibot_msg_callback`。支持文本/图片/语音/文件；**双向通道，无需公网**；每 30s 发 `ping` 心跳，断线自动重连 |
| 智能机器人/应用-回调地址 | 90930 | 后台配 URL+Token+EncodingAESKey；GET 验 URL、POST 收 XML；需自实现验签+AES 解密（有官方加解密库）；需公网 HTTPS |

> 选择：云端/内网均可跑的服务**优先长连接**，省公网 IP、证书、回调加解密；回调地址模式适合已有 HTTPS 网关的企业。

### 2.3 消息回复（怎么答）
| API | path | 说明 |
|---|---|---|
| 机器人回复 `aibot_respond_msg` | 101463 | 长连接回当前会话；支持文本/Markdown/模板卡片/文件/图片/**流式**(10min 内 finish)；限频 30 条/分/会话 |
| 机器人主动推送 `aibot_send_msg` | 101463 | 定时提醒/告警/欢迎语（`enter_chat` 事件后 5s 内回 `aibot_respond_welcome_msg`） |
| 发送应用消息 `message/send` | 90236 | POST `cgi-bin/message/send`，11 种消息类型；`touser` 支持 `@all`(≤1000人/次)；限频：每日 账号数×200 人次/天、单成员 30 次/分 1000 次/时 |

### 2.4 知识库（知识从哪来）
| API | path | 说明 |
|---|---|---|
| 管理企业知识集 `knowledge_base_list/create/detail/add_doc/remove_doc/modify_name/delete` | 100030 | 数据与智能专区 SDK 接口；支持 pdf/xlsx/doc/docx/网页链接；文件类先传「专区临时文件」拿 `file_media_id`。**注意：企微知识集只托管+内置RAG，不对外提供全文/向量检索接口** |
| 上传临时素材 | 101463 / 专区接口 | 回复图片/文件/语音前上传拿 `media_id` |

> 两种用法：①用企微知识集 = 路线 A（企微内置 RAG 回答）；②自建向量库 = 路线 B（企微只收发消息，RAG 全在自己服务）。

### 2.5 对外客服场景（可选扩展）
| API | 模块 | 说明 |
|---|---|---|
| 微信客服 / 客户联系 | 服务端API「客户联系」 | 面向微信用户：客户发起会话→回调事件→读取消息→主动回复；对接同一套 RAG 引擎 |
| 会话内容存档 | 服务端API（付费开通） | 合规留存、质检、知识回流 |

> 本期若仅做企业内部智能客服，**2.1~2.4 已足够**；2.5 是扩展到对外客户时的增量。

---

## 3. 知识库存储与检索方案

### 3.1 技术选型（路线 B 自建）

| 维度 | 推荐 | 备选 | 说明 |
|---|---|---|---|
| 向量数据库 | **Milvus / Zilliz Cloud**（生产级、亿级） | Chroma/Faiss（小规模起步）、pgvector（已有 PG）、腾讯云 VectorDB、ES+向量 | 数据量 <100 万片段用 Chroma/pgvector 起步；上生产换 Milvus |
| Embedding 模型 | **BGE-M3 / bge-large-zh-v1.5**（中文强） | m3e、text-embedding-v3（阿里云）、开源 bge-small | BGE-M3 支持 8192 长文本、多语言、可做稀疏+稠密混合检索 |
| LLM | DeepSeek-V3 / 通义千问 / 混元 | Qwen2.5、Llama3 本地部署 | 中文问答优先 DeepSeek/通义；注重数据隐私可私有化部署 |
| 重排器(Rerank) | BGE-Reranker | cohere-rerank | 检索 Top-50 → 重排取 Top-5，显著提升召回精度 |
| 文档解析 | PyMuPDF / Unstructured / markitdown | LangChain/LlamaIndex Loader | 表格、扫描件需 OCR(PaddleOCR) |

### 3.2 文档切分策略

| 策略 | 说明 |
|---|---|
| 结构感知切分 | 按标题层级（Markdown/PDF 大纲）切块，保持语义完整，避免打断段落 |
| 递归字符切分 | 无结构文本用 `RecursiveCharacterTextSplitter`：chunk_size 300~500 字，overlap 50~80 字 |
| 表格/FAQ | 表格转 Markdown 后整体成块；FAQ 按"一问一答"成对切块 |
| 元数据 | 每块打标：来源文件、章节标题、页码、更新时间，用于引用溯源与过滤 |
| 增量更新 | 按文件 hash 判断变更，只重灌变更块的向量 |

### 3.3 检索策略
- **混合检索**：向量相似度 + 关键词(BM25/ES) 加权融合，BGE-M3 可同时产出稠密/稀疏向量
- **重排**：召回 Top-50 → Reranker 精排 → 取 Top-5 作为上下文
- **知识集兜底**（可选）：检索置信度过低时转「企微知识集」或转人工，避免硬答

---

## 4. 消息处理逻辑（接收→检索→拼接→生成）

### 4.1 接收与前置处理
```
on aibot_msg_callback(body):
  1. 幂等：body.msgid 存入 Redis SETNX，已处理则跳过
  2. 过滤：忽略机器人自身消息；群聊中只处理 @机器人 的消息（content 去 @前缀）
  3. 类型分派：
     - text      → 直接作为 query
     - voice     → 取转文本（语音消息已转文本推送）
     - image/file→ 下载解析（图片走OCR，文件走文档解析）后作为 query 或入库
```

### 4.2 会话管理
- 会话 key：单聊 `single:{userid}`，群聊 `group:{chatid}`
- 存取：Redis 缓存近 N 轮（如 10 轮）`{role, content}`；长会话超窗后截断/总结
- 记录 `req_id`（headers.req_id），回复时透传

### 4.3 RAG 检索
```
def retrieve(query, top_k=5):
    q_vec = embed(query)                       # Embedding 模型
    hits  = vector_db.search(q_vec, top_k=50)  # 向量召回
    if reranker: hits = rerank(query, hits)[:5]
    return hits
```

### 4.4 Prompt 拼接与生成
```
SYSTEM_PROMPT = """你是{企业}的智能客服。请严格依据【知识片段】回答；
若片段无法解答，如实说明"暂未收录，已转人工"，禁止编造。
回答尽量引用片段来源（文件名+章节）。"""
prompt = SYSTEM_PROMPT
       + "\n【历史对话】\n" + recent_turns
       + "\n【知识片段】\n" + "\n---\n".join(top_chunks)
       + "\n【用户问题】\n" + user_query
answer = llm.chat(prompt)   # 可流式生成
```

### 4.5 回复回写
- 长连接：`aibot_respond_msg`（req_id 透传），支持流式 `stream`，分片发送体验更佳
- 兜底：检索无结果/LLM 超时 → 返回引导语 + 转人工（推送客服名片/工单链接）
- 日志：全链路落日志 + 可选埋点，用于后续知识回流（把好问答对补进知识集）

---

## 5. 开发步骤与关键配置项（从零落地）

### 5.1 步骤清单
| 阶段 | 步骤 | 产出 |
|---|---|---|
| M0 准备 | ① 注册企业微信并完成企业认证；② 管理后台创建**自建应用**（拿 corpid/secret/agentid）；③ 创建工作区/服务器，规划 IP 白名单 | 应用凭证 |
| M1 鉴权 | ④ 实现 access_token 获取+缓存+自动刷新（按应用隔离） | token 服务 |
| M2 消息链路 | ⑤ 创建**智能机器人**→开 API 模式→选**长连接**（拿 BotID/Secret）；⑥ 实现 WebSocket 订阅/心跳/断线重连/`aibot_msg_callback` 接收 + `aibot_respond_msg` 回复（先回显 echo 验证） | 消息闭环 |
| M3 知识库 | ⑦ 实现离线管道：文档采集→清洗→切分→Embedding→向量入库（Milvus/Chroma）；⑧ 写知识集/向量库管理脚本（增删改+增量更新） | 向量库 |
| M4 RAG 引擎 | ⑨ 实现 retrieve+rerank；⑩ Prompt 模板 + LLM 调用（接 DeepSeek/通义）；⑪ 流式输出 | 问答闭环 |
| M5 打磨 | ⑫ 会话管理（多轮）；⑬ 转人工/兜底话术；⑭ 引用来源格式化；⑮ 频率控制与错误重试 | 可上线 |
| M6 上线 | ⑯ 日志/监控/告警；⑰ 灰度开放；⑱ 知识回流机制（好问答回填知识集） | 稳定运行 |

### 5.2 关键配置项清单
| 配置 | 取值示例 | 位置 |
|---|---|---|
| corpid | `wwxxxxxxxxxxxx` | 管理后台→我的企业→企业信息 |
| 应用 secret / agentid | `xxx` / `1000002` | 管理后台→应用管理→自建应用 |
| 智能机器人 BotID / Secret | `aibot_xxx` / 长连接专用密钥 | 智能机器人→API 模式 |
| access_token 缓存 | Redis 存 2h，过期前 5min 刷新 | 自建服务 |
| 回调 URL / Token / EncodingAESKey | 仅回调地址模式需要 | 应用设置→接收消息 |
| 向量库连接 | Milvus 主机/端口/集合名 | 自建服务 |
| Embedding 模型 | bge-m3（挂载自研或云端 API） | 自建服务 |
| LLM API Key / Base URL | DeepSeek / 阿里云百炼 | 自建服务 |
| 智能机器人回答模式 | 「仅基于知识库回答」/「结合模型知识」 | 管理后台机器人设置 |
| 转人工关键词 | 如"人工/转人工/投诉"（≤20 个） | 管理后台机器人设置 |
| IP 白名单 | 自建服务出口 IP（调 `getcallbackip` 获取企微 IP 做防火墙放行） | 管理后台/防火墙 |

### 5.3 上线注意（官方文档明确点）
1. access_token 2 小时过期且可能被提前失效，必须实现失效重取，**不可频繁调 gettoken**；
2. 长连接同一机器人同时仅允许一个有效连接，新连接踢旧连接，需做断线重连与心跳；
3. 回复限频：30 条/分/会话、1000 条/时；主动推送注意避开整点 0/30 分资源挤占；
4. 回调模式 5s 超时重试最多 3 次，**先应答再异步处理**，重要业务做对账；
5. 语音消息仅单聊支持且为转文本结果；群聊仅文本可靠，注意降级处理；
6. 机器人只能加**内部群**，暂不支持外部群/互联群/上下游群。

---

## 附录一：路线 A（企微原生智能机器人）操作步骤
1. 企业微信管理后台 → 工作台 → 添加「智能机器人 AI」→ 新建机器人；
2. 填写机器人名称/头像/欢迎语，选择模型（混元/DeepSeek，可勾选深度思考模型）；
3. 配置「知识库」→ 添加知识来源：企微文档/微盘/本地文件/网页/标准问答对（智能表格批量导入）；
4. 设置回答模式：「仅基于知识库回答」或「结合模型自身知识」；配置转人工关键词；
5. 把机器人添加到内部群聊或让员工单聊，@机器人即可问答（多轮最多 10 轮）；
6. 可选：开启「API 模式」接入自有 AI 能力（长连接/回调地址），或通过「管理企业知识集」SDK 用 API 增删知识。
> 优点：零代码、企微托管 RAG/模型/多轮/转人工；缺点：知识集不对外提供检索接口、模型不可选第三方、无流式定制。

## 附录二：目录结构建议（路线 B）
```
rag-support/
├── src/
│   ├── wecom/           # 企微接入：token、长连接/回调、加解密、消息收发
│   ├── knowledge/       # 文档采集、清洗、切分、Embedding、入库/增量
│   ├── rag/             # 检索(混合)、重排、Prompt 模板、LLM 调用
│   ├── chat/            # 会话管理、幂等、路由、转人工
│   └── main.py          # 入口（asyncio）
├── scripts/             # 知识库重建、问答对导入等运维脚本
├── configs/             # config.yaml（企微/模型/向量库配置）
├── tests/
└── requirements.txt     # fastapi/websockets/langchain/faiss-cpu/milvus-pymilvus/...
```

---

*本文档基于企业微信官方开发文档梳理，关键接口与限制均标注了文档 path 以便核对；官方文档可能更新，实施前请以开发者中心最新版本为准。*
