# 企业知识库 + 微信客服对外智能客服系统 —— 技术方案

> 版本：v1.0 ｜ 日期：2026-08-13 ｜ 依据：企业微信开发者中心官方文档
> 场景：**知识在内，服务在外**——企业用自有知识库（产品手册/规章制度/FAQ），通过「微信客服」对外部 C 端客户提供自助咨询，释放人工咨询量。
> 关键文档 path：`91039`(access_token)、`94699`(微信客服-接收消息和事件)、`94677`(微信客服-发送消息)、`90930`(回调配置)

---

## 0. 需求与结论先行

### 0.1 需求定义

| 维度 | 内容 |
|---|---|
| 服务对象 | 微信外部 C 端客户（无需加好友，微信 App 内直接发起会话） |
| 接入渠道 | **微信客服**（企业微信客服账号，`open_kfid`） |
| 知识来源 | **企业自有知识库**（产品技术手册、规章制度、FAQ、官网、工单沉淀等，企业内部自行维护） |
| 核心目标 | **释放人工咨询量（降本）**：高频重复问题由机器人自助解答，复杂/敏感问题转人工 |
| 消息通道 | **回调 + `sync_msg` 拉取 + `send_msg` 发送**（微信客服为拉取模式，非长连接） |
| 关键约束 | **48 小时内最多回复 5 条**（需"新接入待处理/智能助手接待"状态） |
| 安全合规 | **强**：敏感词过滤、内容审核、越权防控、脱敏 |

### 0.2 一句话结论

> 企业自有知识库经离线管道向量化后，由自建 RAG 服务通过「微信客服」渠道对外部 C 端客户提供自助问答；回答聚合为单条高质量内容，命中低置信度/敏感词/客户要求时无缝转人工。整体链路为「知识在内、服务在外」。

### 0.3 技术路线

**单一主线：自建 RAG + 微信客服接入**。企微内置知识集仅作可选辅助源，不作为主线（其不对外提供检索接口，无法满足"企业自有知识库对外"需求）。

---

## 1. 整体架构与全链路流程

```
外部微信客户（C 端用户，微信 App 内）
        │ 发起会话 / 发消息
        ▼
【微信客服】企业微信客服账号（open_kfid）
        │ ① 事件回调（kf_msg_or_event）
        │ ② 拉取消息 sync_msg（cursor 游标 + token 10min 有效）
        │ ③ 发送消息 send_msg（48h 内 ≤5 条）
        ▼
─────────────────────────────────────────────────────
接入层（微信客服开放能力）
  ● 回调 URL（Token + EncodingAESKey 验签解密）
  ● 获取 access_token（corpid + secret）
─────────────────────────────────────────────────────
核心服务层（自建 RAG 服务）
  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
  │ 消息拉取  │▶│ 安全合规  │▶│ 意图/会话 │▶│ RAG 引擎  │▶│ 单条聚合  │
  │(同步轮询) │ │(敏感词/审核│ │(上下文)   │ │(检索增强) │ │ 回答生成  │
  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘
                                                 │低置信度/敏感/要求人工
                                                 ▼
                                          ┌──────────────┐
                                          │ 转人工(分配会话) │
                                          └──────────────┘
─────────────────────────────────────────────────────
知识层（企业自有知识库）
  文档采集 → 清洗 → 切分 → Embedding → 向量库 → 增量更新
```

### 全链路时序

```
客户提问 → ①微信客服回调触发 → ②sync_msg 拉取消息(带 cursor/token)
 → ③验签/幂等(msgid) → ④安全合规(敏感词/越权/审核)
 → ⑤会话管理(取/存上下文, key=open_kfid+external_userid)
 → ⑥RAG 检索(向量化→Top-K→重排) → ⑦Prompt 拼接(系统词+片段+历史+问题)
 → ⑧LLM 生成(聚合为单条回答) → ⑨send_msg 回写(≤5条/48h)
 → ⑩低置信度/敏感/用户要求 → 分配客服会话转人工(带上下文)
```

---

## 2. 需要调用的企业微信 API 清单（微信客服为主）

### 2.1 基础鉴权
| API | path | 作用 |
|---|---|---|
| 获取 access_token | 91039 | 一切接口前置凭证。GET `qyapi.weixin.qq.com/cgi-bin/gettoken`；有效期 7200s；按应用缓存、失效自动重取；**存后端，禁下发前端** |

### 2.2 消息接收（客户问什么）
| API | path | 说明 |
|---|---|---|
| 微信客服-回调配置 | 90930 | 后台配 URL+Token+EncodingAESKey；GET 验 URL、POST 收 `kf_msg_or_event` 事件；需自实现验签+AES 解密（有官方加解密库）；需公网 HTTPS |
| 微信客服-同步消息 `sync_msg` | 94699 | POST `cgi-bin/kf/sync_msg`；入参 `open_kfid`(必填)+`cursor`(游标)+`token`(回调返回，10min 有效)；返回 `next_cursor`+`has_more`+`msg_list`；**消息仅可拉最近 3 天**；`origin=3` 为客户消息 |
| 事件类型 | 94699 | `enter_session`(进会话,可发欢迎语)、`session_status_change`(会话状态)、`servicer_status_change`、`user_recall_msg` 等 |

> **微信客服是"拉取"模式，不是长连接**：回调只通知"有新消息"，内容需主动 `sync_msg` 拉取。需维护 cursor 游标、按 `has_more` 循环拉取，不能靠 `msg_list` 为空判断结束。

### 2.3 消息回复（怎么答）
| API | path | 说明 |
|---|---|---|
| 微信客服-发送消息 `send_msg` | 94677 | POST `cgi-bin/kf/send_msg`；入参 `touser`(external_userid)+`open_kfid`+`msgtype`；支持 text/image/voice/video/file/link/miniprogram/msgmenu/location；text 最长 **2048 字节** |
| 上传临时素材 | 素材接口 | 回复图片/文件/语音前上传拿 `media_id` |

> ⚠️ **核心限制**：客户发消息后，企业 **48 小时内最多回复 5 条**，且仅当客户处于"新接入待处理"或"由智能助手接待"状态。→ 决定我们的回答必须**聚合为单条高质量回答**，不能流式多轮刷屏。

### 2.4 转人工（怎么交给真人）
| API | 模块 | 说明 |
|---|---|---|
| 分配客服会话 | 微信客服-会话分配 | 将客户从"智能助手接待"转给指定接待人员；**转人工时带上会话摘要/上下文**，避免客户重复描述 |
| 发送事件响应消息 | 微信客服 | 响应 `enter_session` 等事件时发送欢迎语 |

### 2.5 知识库（知识从哪来——企业自有）
| 来源 | 说明 |
|---|---|
| 企业自有知识库 | **主线**：产品技术手册、规章制度、FAQ、官网、工单沉淀等，自建离线管道采集入库（见 §3） |
| 企微知识集（可选） | 仅作辅助源，非主线；`100030` 管理知识集 SDK，但不对外提供检索接口，一般不用 |

---

## 3. 知识库存储与检索方案（企业自有）

### 3.1 技术选型（自建 RAG）

| 维度 | 推荐 | 备选 | 说明 |
|---|---|---|---|
| 向量数据库 | **Milvus / Zilliz Cloud** | Chroma/Faiss（起步）、pgvector、腾讯云 VectorDB | <100 万片段用 Chroma/pgvector 起步 |
| Embedding | **BGE-M3 / bge-large-zh-v1.5** | m3e、text-embedding-v3、bge-small | BGE-M3 支持 8192 长文本、混合检索 |
| LLM | DeepSeek-V3 / 通义千问 / 混元 | Qwen2.5、私有化部署 | 中文问答优先 DeepSeek/通义；数据敏感可私有化 |
| 重排 Rerank | BGE-Reranker | cohere-rerank | Top-50 → 重排取 Top-5 |
| 文档解析 | PyMuPDF / Unstructured / markitdown | PaddleOCR | 表格、扫描件需 OCR |

### 3.2 知识库构建策略（面向"对外客户"的差异）

| 策略 | 说明 |
|---|---|
| 分层知识 | 区分「对外可答」与「内部敏感」两类：**对外知识库只入面向客户的公开内容**（产品说明/使用教程/售后政策），内部机密（定价底价、内部制度、员工信息）**不入对外库**，或打敏感标签由合规层拦截 |
| 结构感知切分 | 按标题层级切块；FAQ 按"一问一答"成对切块 |
| 元数据 | 每块打标：来源文件、章节、**可见范围(对外/内部)**、更新时间，用于过滤与溯源 |
| 增量更新 | 按文件 hash 判断变更，只重灌变更块 |

### 3.3 检索策略
- **混合检索**：向量 + 关键词(BM25) 加权；BGE-M3 可同时产出稠密/稀疏向量
- **重排**：召回 Top-50 → Reranker 精排 → Top-5 作上下文
- **置信度阈值**：检索相似度低于阈值 → 不硬答，走转人工/兜底话术

---

## 4. 消息处理逻辑（对外客服版）

### 4.1 接收与前置处理
```
on kf_msg_or_event 回调:
  1. 验签解密 → 取 OpenKfId、Token
  2. sync_msg 拉取(cursor + token)，循环至 has_more=0
  3. 逐条处理 origin=3 的客户消息：
     - 幂等：msgid 存 Redis SETNX，已处理跳过
     - 类型：text 直接作 query；image/voice 走 OCR/ASR 转文本；其他类型降级
```

### 4.2 安全合规层（对外场景新增，关键）
```
安全合规(msg):
  1. 敏感词过滤：命中"报价/底价/内部/员工/领导"等 → 拦截并转人工或给合规话术
  2. 越权防控：客户只能检索「对外可见」知识（可见范围过滤，见 §3.2）
  3. 内容审核：LLM 输出侧做违规词二次过滤，防止模型被诱导泄露内部信息
  4. 脱敏：输出前对手机号/身份证/内部字段做打码
```

### 4.3 会话管理
- 会话 key：`kf:{open_kfid}:{external_userid}`
- 存取 Redis 近 N 轮 `{role, content}`；**受 48h/5 条限制，建议精简上下文，聚合回答**
- 记录 `msgid` 作幂等；转人工时把上下文摘要一并交接

### 4.4 RAG 检索与生成（单条聚合）
```
SYSTEM_PROMPT = """你是{企业}的对外智能客服。仅依据【知识片段】回答客户问题，
一次给出完整、简洁的答案（避免多轮追问，因回复条数受限）。
片段无法解答时，引导转人工，禁止编造；不透露内部敏感信息。"""
prompt = SYSTEM_PROMPT + 历史 + 知识片段(top_chunks) + 用户问题
answer = llm.chat(prompt)   # 聚合为单条，≤2048 字节，超出精炼
```

### 4.5 回复与转人工
- **正常**：`send_msg` 单条回答（控制 ≤5 条/48h，优先一次说清）
- **转人工触发条件**：检索低置信度 / 命中敏感词 / 用户明确说"人工/转人工/投诉" / 多次无法解答
- **转人工动作**：调用「分配客服会话」转交真人，并推送会话摘要（客户问题 + 机器人已答内容）
- **日志埋点**：全链路日志 + 命中/转人工率统计，用于知识回流与效果评估

---

## 5. 开发步骤与关键配置（从零落地）

### 5.1 步骤清单
| 阶段 | 步骤 | 产出 |
|---|---|---|
| M0 准备 | ① 注册企业微信并认证；② 创建自建应用（corpid/secret）；③ 开通「微信客服」并创建客服账号(open_kfid)，把应用配置到「可调用接口的应用」；④ 部署服务 + 公网 HTTPS + IP 白名单 | 凭证/客服账号 |
| M1 鉴权 | ⑤ access_token 获取+缓存+自动刷新 | token 服务 |
| M2 消息链路 | ⑥ 配置回调 URL+Token+EncodingAESKey；⑦ 实现验签解密 + `sync_msg` 拉取(游标循环) + `send_msg` 发送（先 echo 验证） | 消息闭环 |
| M3 知识库 | ⑧ 离线管道：采集→清洗→切分→Embedding→入库；⑨ 标注「对外可见范围」元数据；⑩ 增量更新脚本 | 对外向量库 |
| M4 RAG | ⑪ retrieve+rerank；⑫ Prompt + LLM（单条聚合）；⑬ 置信度阈值 | 问答闭环 |
| M5 合规+转人工 | ⑭ 敏感词过滤+越权过滤+输出审核+脱敏；⑮ 转人工(分配会话+上下文交接) | 可上线 |
| M6 上线 | ⑯ 日志/监控/告警；⑰ 命中率/转人工率看板；⑱ 知识回流（高频未命中问题回填） | 稳定运行 |

### 5.2 关键配置项
| 配置 | 取值示例 | 位置 |
|---|---|---|
| corpid | `wwxxxxxxxxxxxx` | 管理后台→我的企业 |
| 应用 secret | `xxx` | 管理后台→应用管理→自建应用 |
| 客服账号 open_kfid | `wkxxxxxxx` | 微信客服→客服账号 |
| 回调 URL / Token / EncodingAESKey | 公网 HTTPS | 微信客服→开发配置 |
| access_token 缓存 | Redis 2h，过期前 5min 刷新 | 自建服务 |
| 向量库 / Embedding / LLM | Milvus / bge-m3 / DeepSeek | 自建服务 |
| 对外知识可见范围标记 | 元数据字段 `visibility=external` | 知识库 |
| 敏感词库 | "底价/内部/员工/领导…" | 合规层 |
| 转人工关键词 | "人工/转人工/投诉" | 合规层 |

### 5.3 上线注意（微信客服特有约束，务必核对）
1. **48 小时 5 条限制**：客户发消息后 48h 内最多回 5 条，且需"新接入待处理/智能助手接待"状态 → 回答务必单条聚合、一次说清；
2. **sync_msg 是拉取模式**：必须持久化 `next_cursor`，按 `has_more` 循环拉取，不能靠 `msg_list` 空判断结束；消息仅可拉最近 3 天；
3. **token 10 分钟有效**：回调返回的 token 仅 10min，需及时消费，否则改走无 token 的严格限频；
4. `send_msg` 返回成功≠真正送达，需监听 `msg_send_fail` 事件（如"会话超 48h/已关闭/超 5 条"）；
5. 媒体大小限制：图片 2MB / 语音 2MB / 视频 10MB / 文件 20MB；
6. 自建应用需配置到「微信客服-可调用接口的应用」，接待人员需在应用可见范围内；
7. 对外服务需关注**内容合规**：敏感词、越权、脱敏是必做项，否则有泄露内部信息的风险。

---

## 附录一：目录结构建议
```
rag-kf-support/
├── src/
│   ├── wecom_kf/         # 微信客服接入：token、回调验签、sync_msg 拉取、send_msg、转人工
│   ├── compliance/       # 安全合规：敏感词、越权过滤、输出审核、脱敏
│   ├── knowledge/        # 企业知识采集、清洗、切分、Embedding、对外可见标注、增量
│   ├── rag/              # 检索(混合)、重排、Prompt、LLM 单条聚合
│   ├── chat/             # 会话管理、幂等、路由、转人工交接
│   └── main.py           # 入口（asyncio）
├── scripts/              # 知识库重建、敏感词维护、问答对导入
├── configs/              # config.yaml（企微/模型/向量库/合规规则）
├── tests/
└── requirements.txt
```

---

*本文档基于企业微信官方开发文档梳理，关键接口与限制均标注文档 path 以便核对；官方文档可能更新，实施前请以开发者中心最新版本为准。*
