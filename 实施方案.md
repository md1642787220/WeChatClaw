# 对外智能客服系统 —— 实施计划

> 版本：v1.0 ｜ 日期：2026-08-13
> 关联方案：`企业微信RAG智能客服技术方案.md`（技术方案）
> 本文档从工程落地角度，规划从项目骨架搭建到部署上线的完整实施路径。

---

## 0. 概述

本计划围绕「企业自有知识库 + 微信客服对外服务 + 机器人优先/人工兜底」的目标，将开发拆分为 6 个阶段（M0～M5），每个阶段给出明确目标、任务清单、交付物与验收标准。

**总体技术栈**：Python 3.11+ / FastAPI / asyncio / Redis / Milvus（或 Chroma 起步）/ BGE-M3 Embedding / DeepSeek（或通义）LLM / 企业微信服务端 API。

---

## 1. 阶段总览

| 阶段 | 名称 | 目标 | 预计周期 | 关键交付物 |
|---|---|---|---|---|
| M0 | 项目骨架 | 仓库结构、依赖、配置、CI 就绪 | 0.5 周 | 可运行空服务 + 配置框架 |
| M1 | 接入层与鉴权 | 打通企微鉴权与消息收发闭环 | 1 周 | access_token 服务、回调验签、sync_msg/send_msg 封装 |
| M2 | 知识库管道 | 企业知识入库可检索 | 1 周 | 离线管道 + 向量库 + 对外可见标注 |
| M3 | RAG 问答引擎 | 完成"检索→生成→回复"问答闭环 | 1 周 | RAG 检索、Prompt、LLM 单条聚合回答 |
| M4 | 安全合规与转人工 | 对外安全可控 + 人工兜底 | 1 周 | 敏感词/越权/审核/脱敏 + 转人工 |
| M5 | 测试与部署 | 稳定上线、可观测、可运维 | 1 周 | 单测/集成测试、部署、监控看板 |

> 合计约 5.5 周，可并行压缩至 4 周（M1 与 M2 可并行）。

---

## 2. M0 项目骨架（0.5 周）

### 目标
建立可运行、可测试、可扩展的工程骨架，为后续模块提供统一约定。

### 任务清单
1. 初始化仓库与目录结构（见 §7）
2. 配置 `pyproject.toml`（依赖声明 + uv/pip 锁定）+ `requirements.txt`
3. 引入 `pydantic-settings` 配置框架，编写 `configs/config.yaml` 与配置模型
4. 建立日志规范（`structlog`/`logging` + 轮转）
5. 配置 CI（GitHub Actions / 内部流水线）：lint（ruff/mypy）+ 单测（pytest）
6. 编写 `main.py` 入口（FastAPI + uvicorn）跑通健康检查 `/healthz`

### 交付物
- 可启动的空服务，`GET /healthz` 返回 200
- 配置/日志/依赖/CI 基线就绪

### 验收标准
- 本地 `uv run uvicorn src.main:app` 成功启动
- CI 流水线全绿

---

## 3. M1 接入层与鉴权（1 周）

### 目标
打通企业微信鉴权与消息收发闭环，实现「客户发消息 → 服务收到 → echo 回显」。

### 任务清单
1. **鉴权**：实现 `access_token` 获取 + Redis 缓存 + 失效自动刷新（按应用隔离）
2. **回调验签**：实现 URL 验证（GET）与消息验签解密（POST），集成官方加解密库
3. **消息拉取**：封装 `sync_msg`（`94699`），持久化 `next_cursor`，按 `has_more` 循环拉取，处理 token 10min 有效期
4. **消息发送**：封装 `send_msg`（`94677`），支持 text 及基础类型
5. **幂等**：`msgid` 用 Redis SETNX 去重
6. **联调**：配置回调 URL + 客服账号，真实环境 echo 验证

### 交付物
- `wecom_kf` 模块：token / 回调 / sync_msg / send_msg 封装
- 真实企微环境跑通 echo 闭环

### 验收标准
- 客户在微信发消息，服务收到并原样回一条文本
- cursor 游标持续正确、无重复/丢消息

---

## 4. M2 知识库管道（1 周）

### 目标
将企业自有知识文档清洗、切分、向量化入库，支持按「对外可见范围」过滤检索。

### 任务清单
1. **文档采集**：支持 PDF/Word/Markdown/Excel/网页（PyMuPDF / Unstructured / markitdown）
2. **文本清洗**：页眉页脚去除、表格转 Markdown、OCR（扫描件，PaddleOCR）
3. **切分**：结构感知切分（标题层级）+ 递归字符切分（chunk 300~500 字，overlap 50~80 字）+ FAQ 成对切块
4. **Embedding**：BGE-M3 向量化
5. **入库**：Milvus/Chroma 写入 + 元数据（来源文件、章节、**可见范围 external/internal**、更新时间）
6. **增量更新**：文件 hash 判断变更，重灌变更块
7. **管理脚本**：知识库增删改查、批量导入 CLI

### 交付物
- `knowledge` 模块 + 入库/增量脚本
- 一批示例知识已入库可检索

### 验收标准
- 指定文档入库后，按关键词/语义可检索到正确片段
- internal 片段在对外检索中不可见

---

## 5. M3 RAG 问答引擎（1 周）

### 目标
完成"检索 → 生成 → 回复"问答闭环，输出单条高质量聚合回答。

### 任务清单
1. **检索**：混合检索（向量 + BM25）+ Reranker 重排（Top-50 → Top-5）
2. **置信度阈值**：相似度低于阈值判为低置信度（触发转人工）
3. **Prompt 模板**：系统提示词（对外客服角色、单条完整回答、禁编造、不泄内部信息）
4. **LLM 调用**：接入 DeepSeek/通义，支持超时与重试
5. **单条聚合**：回答 ≤2048 字节，超出精炼
6. **会话管理**：Redis 存近 N 轮上下文（key = open_kfid + external_userid），受 48h/5 条限制精简

### 交付物
- `rag` + `chat` 模块
- 端到端问答跑通

### 验收标准
- 输入典型客户问题，返回基于知识片段的准确回答，且带来源引用
- 无对应知识时不编造，给出转人工引导

---

## 6. M4 安全合规与转人工（1 周）

### 目标
确保对外服务安全可控，实现机器人优先 + 人工兜底。

### 任务清单
1. **敏感词过滤**：命中"底价/内部/员工/领导"等 → 拦截，转人工或给合规话术
2. **越权防控**：检索仅限 `visibility=external` 知识
3. **输出审核**：LLM 输出侧违规词二次过滤 + 脱敏（手机号/身份证/内部字段打码）
4. **转人工**：低置信度 / 敏感词 / 用户要求 → 调用「分配客服会话」转交，并推送会话摘要
5. **欢迎语**：`enter_session` 事件响应欢迎语
6. **日志埋点**：命中率、转人工率、未命中问题统计

### 交付物
- `compliance` 模块 + 转人工流程
- 敏感词库、合规规则配置

### 验收标准
- 命中敏感词的问题不会泄露内部信息
- 低置信度/用户要求时可正确转人工并交接上下文

---

## 7. M5 测试与部署（1 周）

### 目标
保证质量与稳定性，完成部署上线，具备可观测性。

### 任务清单
1. **单元测试**：各模块单测（token、幂等、切分、检索、合规、转人工）
2. **集成测试**：mock 企微 API 跑通全链路
3. **部署**：容器化（Docker）+ 编排（Docker Compose / K8s），配置公网 HTTPS 反代
4. **可观测**：日志采集 + 指标（Prometheus）+ 告警
5. **数据看板**：命中率 / 转人工率 / 高频未命中问题
6. **知识回流**：未命中高频问题回填知识库
7. **灰度**：先小范围放量，观察 48h/5 条与 `msg_send_fail` 事件

### 交付物
- 测试通过 + 部署文档 + 监控看板
- 上线运行

### 验收标准
- 单测/集成测试通过，CI 全绿
- 生产环境稳定运行，命中率、转人工率可观测
- 满足《技术方案 §5.3》全部上线约束（48h/5 条、cursor 持久化、token 10min、msg_send_fail）

---

## 8. 里程碑与依赖关系

```
M0 骨架
  └─▶ M1 接入层 ─┐
  │              ├─▶ M3 RAG ─▶ M4 合规/转人工 ─▶ M5 测试部署
  └─▶ M2 知识库 ─┘
```

- M1、M2 无相互依赖，可并行
- M3 依赖 M1（消息链路）+ M2（可检索知识）
- M4 依赖 M3（问答闭环）
- M5 依赖 M4

---

## 9. 目录结构

```
rag-kf-support/
├── src/
│   ├── wecom_kf/         # 微信客服接入：token、回调验签、sync_msg 拉取、send_msg、转人工
│   ├── compliance/       # 安全合规：敏感词、越权过滤、输出审核、脱敏
│   ├── knowledge/        # 企业知识采集、清洗、切分、Embedding、对外可见标注、增量
│   ├── rag/              # 检索(混合)、重排、Prompt、LLM 单条聚合
│   ├── chat/             # 会话管理、幂等、路由、转人工交接
│   └── main.py           # 入口（FastAPI + asyncio）
├── scripts/              # 知识库重建、敏感词维护、问答对导入
├── configs/              # config.yaml（企微/模型/向量库/合规规则）
├── tests/
├── docker/               # Dockerfile / compose
├── pyproject.toml
└── requirements.txt
```

---

## 10. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 48h/5 条限制导致多轮体验差 | 高 | 单条聚合回答；Prompt 引导一次问清；必要时用菜单/图文替代追问 |
| sync_msg 拉取丢消息 | 高 | cursor 持久化 + 幂等 + 按 has_more 循环 + 消息对账 |
| LLM 输出泄露内部信息 | 高 | 知识库只入 external 子集 + 输出审核 + 脱敏 + 敏感词拦截 |
| 转人工无上下文，客户重复描述 | 中 | 转人工时推送会话摘要 |
| Embedding/向量库成本 | 中 | 起步用 Chroma，规模化换 Milvus；按量计费评估 |
| 合规风险（内容审核） | 高 | 接入内容审核（可选第三方） + 敏感词库持续维护 |

---

*本文档随开发进度持续更新；各阶段验收标准未达成前，不进入下一阶段。*
