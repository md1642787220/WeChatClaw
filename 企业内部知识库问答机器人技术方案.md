# 企业内部知识库问答机器人 —— 技术方案（LangChain 版）

> 版本：v2.0 ｜ 日期：2026-08-13
> 场景：企业员工就「规章制度 / 产品知识 / 业务流程 / FAQ」等内部知识提问，机器人基于企业自有知识库检索并生成回答，降低内部答疑成本、沉淀知识复用。
> 框架：**LangChain（v1.2.x）** + LangGraph，采用模块化分包与 LCEL 链式编排。
> 定位：**核心能力 = RAG 智能问答引擎**，通过独立网页提供问答服务。

---

## 0. 需求与结论先行

### 0.1 需求定义

| 维度 | 内容 |
|---|---|
| 服务对象 | 企业**内部员工** |
| 知识来源 | 企业**自有内部知识库**（规章制度、产品手册、流程文档、FAQ、过往工单沉淀） |
| 核心目标 | **降本增效**：员工高频重复问题由机器人自助解答，释放人力、知识可复用 |
| 接入渠道 | **独立网页** |
| 安全合规 | 内部知识需**权限分级**，敏感内容做可见范围控制与脱敏 |

### 0.2 一句话结论

> 企业自有内部文档经 LangChain 管道切分、向量化后入库，由基于 LCEL 编排的 RAG 链进行「检索 + 增强生成」，输出有据可依、可溯源的答案。

### 0.3 技术路线

**单一主线：LangChain 构建 RAG 引擎 + 网页入口**。

---

## 1. 整体架构

```
企业员工（浏览器访问）
        │ 输入问题
        ▼
【网页前端】聊天界面（HTML + 原生 JS）
        │ POST /chat
        ▼
─────────────────────────────────────────────
问答服务层（FastAPI）
  ┌──────────┐  ┌──────────┐  ┌────────────────┐
  │ 会话管理  │▶│ 安全合规  │▶│ RAG 链（LCEL）  │
  │(上下文/幂等)│ │(敏感词/脱敏)│ │(检索+重排+生成) │
  └──────────┘  └──────────┘  └────────────────┘
                                │
                                ▼
                          ┌──────────────┐
                          │ ChatModel 生成 │
                          └──────────────┘
─────────────────────────────────────────────
知识层（企业自有知识库）
  文档加载(Loader) → 切分(Splitter) → Embedding → 向量库(Chroma) → 增量更新
```

### 全链路时序

```
员工提问 → 网页 POST /chat
 → 会话管理（取/存上下文）
 → 安全合规（敏感词 / 越权过滤）
 → RAG 链（retrieve → rerank → format → prompt → llm）
 → 生成答案（有据可依，附带出处）
 → 返回前端展示（答案 + 引用来源）
```

---

## 2. LangChain 模块化选型

> LangChain 采用**模块化分包**，按需引入，避免引入庞大的全量 `langchain` 包。

| 包 | 作用 | 对应环节 |
|---|---|---|
| `langchain-core` | 核心抽象：Runnable / LCEL / PromptTemplate / Document | 链式编排、模板 |
| `langchain-text-splitters` | 文本切分器 | 知识库切分 |
| `langchain-chroma` | Chroma 向量库集成 | 向量存储 / 检索 |
| `langchain-openai` | OpenAI 兼容协议（DeepSeek 走 OpenAI 协议） | ChatModel + Embeddings |
| `langchain-community` | 文档加载器（Markdown/PDF/Word）等社区组件 | 文档加载 |
| `langgraph` | 图引擎（可选，复杂流程编排） | 预留：多步推理 / 路由 |

### 2.1 为什么选 LCEL（LangChain 表达式语言）

- 用 `|` 管道符把组件串联，声明式、可读性强
- 内置 `RunnableParallel` / `RunnablePassthrough` 支持并行与透传
- 统一 `invoke / stream / batch` 执行协议，便于流式输出

---

## 3. 知识库管道设计（knowledge/）

### 3.1 处理流程（LangChain Ingestion）

```
文档加载(Loader) → 清洗 → 切分(Splitter) → Embedding → 向量库入库 → 增量更新
```

### 3.2 组件选型

| 步骤 | LangChain 组件 | 说明 |
|---|---|---|
| 加载 | `TextLoader` / `UnstructuredMarkdownLoader` | 首期 Markdown/txt；PDF 用 `PyPDFLoader`、Word 用 `Docx2txtLoader` |
| 切分 | `RecursiveCharacterTextSplitter` | 按标题层级 + 递归字符切分 |
| 向量化 | `langchain-openai` Embeddings（或本地 bge） | DeepSeek 无 embedding 时用本地模型 |
| 存储 | `langchain-chroma` `Chroma` | 本地持久化，`as_retriever()` 直接生成检索器 |

### 3.3 元数据与权限

| 字段 | 说明 |
|---|---|
| `source` | 来源文件路径 |
| `section` | 章节标题 |
| `visibility` | 权限级别：`public`（全员可见）/ `restricted`（敏感，仅授权角色） |
| `updated_at` | 更新时间，用于增量判断 |

> Chroma 支持 `metadata` 过滤，检索时可 `filter={"visibility": "public"}` 实现越权控制。

### 3.4 增量更新

- 按文件 `hash` 判断变更，只重灌变更文件
- 删除已不存在文件对应的向量块

---

## 4. RAG 引擎设计（rag/）—— LCEL 链

### 4.1 标准 RAG 链

```python
from langchain_core.runnables import RunnablePassthrough, RunnableParallel
from langchain_core.output_parsers import StrOutputParser

def format_docs(docs):
    return "\n\n".join(doc.page_content for doc in docs)

rag_chain = (
    {
        "context": retriever | format_docs,
        "question": RunnablePassthrough(),
    }
    | prompt
    | llm
    | StrOutputParser()
)
```

### 4.2 检索策略

| 策略 | 说明 |
|---|---|
| 向量检索 | `Chroma.as_retriever(search_kwargs={"k": 5})` |
| 混合检索 | 后续引入 BM25 + 向量加权（可扩展 `EnsembleRetriever`） |
| 重排 | 可选 `ContextualCompressionRetriever` + bge-reranker |

### 4.3 置信度阈值

- 检索相似度低于阈值 → 不硬答，返回「未找到相关答案」兜底话术
- 避免「一本正经地编造」——企业知识库问答红线

### 4.4 Prompt 模板

```python
from langchain_core.prompts import ChatPromptTemplate

template = """你是企业内部知识库助手。仅依据【知识片段】回答，
回答需准确、简洁，并注明依据的知识来源。
若片段不足以回答，请明确说明"当前知识库中未找到相关信息"，禁止编造。

【知识片段】
{context}

【历史对话】
{history}

【员工问题】
{question}
"""
prompt = ChatPromptTemplate.from_template(template)
```

### 4.5 降级策略（无 LLM Key 时）

- 不做生成，改为返回命中知识片段原文 + 出处
- 接口保持一致，填入 Key 后升级为生成式回答

---

## 5. 安全合规层（compliance/）

| 能力 | 说明 |
|---|---|
| 敏感词过滤 | 命中内部敏感词 → 拦截，返回合规话术 |
| 越权过滤 | 检索时 `filter` 限定 `visibility=public`（或当前角色可见范围） |
| 输出脱敏 | 手机号 / 身份证 / 薪资等字段打码 |
| 输出审核 | LLM 输出侧二次过滤违规内容 |

---

## 6. 会话管理（chat/）

| 维度 | 说明 |
|---|---|
| 会话标识 | 前端 `session_id`，服务端关联上下文 |
| 上下文 | 近 N 轮 `{role, content}`，超长自动截断 |
| 存储 | 首期内存版，后续可换 Redis |
| 幂等 | 记录消息 ID，避免重复处理 |

---

## 7. 开发阶段划分

| 阶段 | 内容 | 产出 |
|---|---|---|
| P0 骨架 | FastAPI 入口 + 配置 + 目录结构 | 可启动服务 |
| P1 知识库管道 | Loader → Splitter → Embedding → Chroma 入库 | 向量库 |
| P2 RAG 引擎 | LCEL 链：retrieve → format → prompt → LLM/降级 | 问答闭环 |
| P3 网页前端 | 聊天界面 + /chat 对接 | 可演示 |
| P4 合规 | 敏感词 / 脱敏 / 越权过滤 | 安全可用 |
| P5 工具 | 文档导入 / 索引重建脚本 | 易维护 |

---

## 8. 目录结构

```
rag-kf-support/
├── src/
│   ├── knowledge/         # Loader、Splitter、Embedding、Chroma 入库、增量
│   ├── rag/               # LCEL 链：检索、重排、Prompt、LLM 生成
│   ├── compliance/        # 敏感词、越权、脱敏、输出审核
│   ├── chat/              # 会话管理、幂等
│   ├── web/               # 网页前端（静态资源）
│   ├── config.py          # 配置加载
│   ├── logging_conf.py
│   └── main.py            # FastAPI 入口（/chat /healthz）
├── scripts/               # 文档导入、索引重建
├── data/                  # 原始文档、向量库持久化目录
├── configs/               # config.yaml
└── tests/
```

---

## 9. 关键配置项

| 配置 | 取值示例 | 位置 |
|---|---|---|
| 向量库 | `chroma` | `config.yaml` |
| Embedding | `BAAI/bge-small-zh-v1.5`（本地）/ OpenAI 协议 | `config.yaml` |
| LLM provider | `deepseek`（OpenAI 兼容） | `config.yaml` |
| LLM API Key | 环境变量 `LLM_API_KEY` | `.env` |
| 敏感词库 | 内部敏感词列表 | `config.yaml` |
| 检索阈值 / Top-K | `threshold=0.5` / `top_k=5` | `config.yaml` |

---

## 10. 快速开始

```bash
# 安装依赖
uv sync --extra rag --extra dev

# 导入示例文档，构建向量库
uv run python -m scripts.build_index

# 启动服务
uv run uvicorn src.main:app --host 0.0.0.0 --port 8000

# 打开网页聊天
# 浏览器访问 http://localhost:8000
```

---

## 11. 依赖规划（LangChain 模块化）

```toml
[project.optional-dependencies]
rag = [
    "langchain-core>=0.3",
    "langchain-text-splitters>=0.3",
    "langchain-chroma>=0.2",
    "langchain-openai>=0.3",
    "langchain-community>=0.3",
    "langgraph>=0.2",
    "chromadb>=0.5",
    "sentence-transformers>=3.0",
]
```

---

*本方案为「企业内部知识库问答机器人」主方案，采用 LangChain（LCEL）构建 RAG 引擎，通过独立网页提供问答服务。*
